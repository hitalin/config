# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
# Mod4 is set by .Xmodmap
set $mod Mod4

# https://github.com/catppuccin/i3
set $rosewater #f5e0dc
set $flamingo  #f2cdcd
set $pink      #f5c2e7
set $mauve     #cba6f7
set $red       #f38ba8
set $maroon    #eba0ac
set $peach     #fab387
set $green     #a6e3a1
set $teal      #94e2d5
set $sky       #89dceb
set $sapphire  #74c7ec
set $blue      #89b4fa
set $lavender  #b4befe
set $text      #cdd6f4
set $subtext1  #bac2de
set $subtext0  #a6adc8
set $overlay2  #9399b2
set $overlay1  #7f849c
set $overlay0  #6c7086
set $surface2  #585b70
set $surface1  #45475a
set $surface0  #313244
set $base      #1e1e2e
set $mantle    #181825
set $crust     #11111b

# https://thomashunter.name/i3-configurator/
# class                  border  bground text   indicator  child_border
client.focused           $lavender $base $text  $rosewater $lavender
client.focused_inactive  $overlay0 $base $text  $rosewater $overlay0
client.unfocused         $overlay0 $base $text  $rosewater $overlay0
client.urgent            $peach    $base $peach $overlay0  $peach
client.placeholder       $overlay0 $base $text  $overlay0  $overlay0
client.background        $base

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:IBM Plex Sans 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:IBM Plex Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- sh $HOME/config/i3lock-color/lock.sh --nofork

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id wezterm

# start emacs in GUI
bindsym $mod+e exec --no-startup-id "emacsclient -c $@ &"

# start bemoji (a emoji picker)
bindsym $mod+Shift+b exec bemoji -t

# start todo.fi (a task picker)
bindsym $mod+Shift+t exec $HOME/dotfiles/bin/todofi.sh

# start rofi (a program launcher)
bindsym $mod+d exec --no-startup-id "rofi -combi-modi drun,run -show combi"
bindsym $mod+g exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'
bindsym $mod+Shift+d exec --no-startup-id rofi -show window
bindsym Mod1+Tab exec --no-startup-id rofi -show window
# Mod1 is set by .Xmodmap
# kill focused window
# https://qiita.com/gyu-don/items/d61b03e0222a7f1ce9f7#%E5%93%80%E3%82%8C%E3%81%AAwindows%E3%83%A6%E3%83%BC%E3%82%B6%E3%81%AE%E3%81%9F%E3%82%81%E3%81%ABaltf4%E3%81%A7%E3%82%A6%E3%82%A3%E3%83%B3%E3%83%89%E3%82%A6%E3%82%92%E9%96%89%E3%81%98%E3%82%8B%E3%82%88%E3%81%86%E3%83%90%E3%82%A4%E3%83%B3%E3%83%89=
bindsym Mod1+F4 kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+x layout toggle split

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show, move position center

# Show/Hide the keepassxc window
# Don't duplicate this setting to [GUI] in ~/.config/keepassxc/keepassxc.ini
bindsym $mod+Shift+p [instance="keepassxc"] scratchpad show, move position center
for_window [instance="keepassxc"] floating enable, move scratchpad, border pixel 1

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# https://qiita.com/gyu-don/items/d61b03e0222a7f1ce9f7#%E5%93%80%E3%82%8C%E3%81%AAwindows%E3%83%A6%E3%83%BC%E3%82%B6%E3%81%AE%E3%81%9F%E3%82%81%E3%81%ABaltf4%E3%81%A7%E3%82%A6%E3%82%A3%E3%83%B3%E3%83%89%E3%82%A6%E3%82%92%E9%96%89%E3%81%98%E3%82%8B%E3%82%88%E3%81%86%E3%83%90%E3%82%A4%E3%83%B3%E3%83%89=
bindsym $mod+Prior workspace prev
bindsym $mod+Next workspace next

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# https://qiita.com/gyu-don/items/d61b03e0222a7f1ce9f7#modshifte%E3%82%92%E6%8A%BC%E3%81%97%E3%81%9F%E3%81%A8%E3%81%8D%E3%81%AE%E7%B5%82%E4%BA%86%E3%82%B3%E3%83%9E%E3%83%B3%E3%83%89%E3%82%92%E8%B1%AA%E8%8F%AF%E3%81%AB%E3%81%99%E3%82%8B=
bindsym $mod+Shift+e exec --no-startup-id \
        "i3-nagbar -t warning -m 'Do you really want to exit i3?' -b 'Shutdown' 'systemctl poweroff' -b 'Reboot' 'systemctl reboot' -b 'Logout' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command SCRIPT_DIR=$HOME/.config/i3blocks i3blocks
        position bottom
        font pango:IBM Plex Sans 10
# https://thomashunter.name/i3-configurator/
        colors {
            background $base
            statusline #FFFFFF
            separator  #666666

            focused_workspace  #4C7899 #285577 $base
            active_workspace   #333333 #222222 $base
            inactive_workspace #333333 #222222 #888888
            urgent_workspace   #2F343A #900000 $base
            binding_mode       #2F343A #900000 $base
        }

}

# for only i3-gaps
gaps outer 2
gaps inner 2

smart_gaps on
smart_borders no_gaps

# hide window title bar
for_window [class="^.*"] border pixel 1
#new_window 1pixel

# startup
## GUI apps in tray
exec --no-startup-id sh $HOME/config/feh/fehbg
exec --no-startup-id picom -b --config $HOME/config/picom/picom.conf
exec --no-startup-id udiskie --tray
#exec --no-startup-id blueman-tray
exec --no-startup-id fcitx5
exec --no-startup-id dunst
exec --no-startup-id greenclip daemon
exec --no-startup-id dropbox
exec --no-startup-id megasync
exec --no-startup-id mullvad-vpn
#exec --no-startup-id megasync
exec --no-startup-id rescuetime
## GUI apps in scratchpad
exec --no-startup-id keepassxc

## GUI app

## GUI apps in workspaces
exec --no-startup-id wezterm
for_window [class="WezTerm"] move to workspace $ws1

#exec --no-startup-id dex -v $HOME/.config/autostart-i3/firefox.desktop
#for_window [class="firefox"] move to workspace $ws2
#exec --no-startup-id thunderbird
#for_window [class="thunderbird"] move to workspace $ws2

#exec --no-startup-id dex -v $HOME/.config/autostart-i3/code.desktop
#for_window [class="Code"] move to workspace $ws3

#exec --no-startup-id obsidian
#for_window [class="obsidian"] move to workspace $ws4

#exec --no-startup-id logseq
#for_window [class="Logseq"] move to workspace $ws5

#exec --no-startup-id dex -v $HOME/.config/autostart-i3/Focus_To-Do.desktop
#exec --no-startup-id dex -v $HOME/.config/autostart-i3/LINE.desktop
#exec --no-startup-id dex -v $HOME/.config/autostart-i3/clickup.desktop
#exec --no-startup-id dex -v $HOME/.config/autostart-i3/linear.desktop
#exec --no-startup-id dex -v $HOME/.config/autostart-i3/todoist.desktop
#exec --no-startup-id dex -v $HOME/.config/autostart-i3/misskey.desktop
exec --no-startup-id dex -v $HOME/.config/autostart-i3/brave.desktop
for_window [class="Brave-browser"] move to workspace $ws6

#exec --no-startup-id dex -v $HOME/.config/autostart-i3/discord.desktop
#for_window [class="discord"] move to workspace $ws7

#exec --no-startup-id dex -v $HOME/.config/autostart-i3/slack.desktop
#for_window [class="Slack"] move to workspace $ws8

#exec --no-startup-id dex -v $HOME/.config/autostart-i3/TelegramDesktop.desktop
#for_window [class="TelegramDesktop"] move to workspace $ws9
#exec --no-startup-id dex -v $HOME/.config/autostart-i3/signal-desktop.desktop
#for_window [class="Signal"] move to workspace $ws9
#exec --no-startup-id pidgin
#for_window [class="Pidgin"] move to workspace $ws9
