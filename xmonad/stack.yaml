# XMonad Stack Configuration
# Updated for LTS 23.28 (GHC 9.8.4)

resolver: lts-23.28

packages:
  - .

# xmobar is not in LTS 23.28, so we need to specify it as extra-dep
extra-deps:
  - xmobar-0.48.1

# Nix integration (uncomment if using NixOS)
# nix:
#   enable: true
